using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Habitat : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void CreateFirstPopulation()
    {
        //Generate N numbers with random DNAs
    }

    public void CreateNextDayFood()
    {
        //Clear current foods and generate foods on random locations in spawn area
    }

    public void ResetBeeAfterOneDay()
    {
        
    }
}
